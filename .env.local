# Microsoft Clarity API トークン
CLARITY_API_TOKEN=

# JWT 署名鍵（Vercel に設定し、外部に漏らさないこと）
# 生成: openssl rand -base64 32
JWT_SECRET=

# JWT ソルト（Vercel に設定し、外部に漏らさないこと）
# 生成: openssl rand -base64 16
JWT_SALT=

# MCP サーバー認証トークン（generate-token.js が自動更新）
MCP_SERVER_AUTH_TOKEN=
